{
  "name": "api-gateway-proxy-lambda",
  "version": "0.2.2",
  "description": "AWS Lambda to proxy calls to specific host",
  "main": "index.js",
  "scripts": {
    "test": "nyc mocha && nyc check-coverage --lines 90 --functions 85 --branches 80",
    "~test": "mocha --watch",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/terma/api-gateway-proxy-lambda"
  },
  "keywords": [
    "nodejs",
    "aws",
    "api gateway",
    "proxy",
    "lambda"
  ],
  "author": "terma (Artem Stasiuk)",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^3.0.0",
    "mocha": "^4.0.1",
    "nock": "^9.1.0",
    "nyc": "^11.4.1"
  }
}
